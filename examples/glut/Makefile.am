LDADD = @GLUT_LIBS@ @GL_LIBS@ -lsh
AM_LDFLAGS = -L$(libdir)
INCLUDES = -I$(includedir)

EXTRA_DIST = README

bin_PROGRAMS = shglutex
shglutex_SOURCES = main.cpp Trackball.cpp Trackball.hpp Camera.cpp Camera.hpp
