LDADD = -lglut -lGL -lGLU -lsh
AM_LDFLAGS = -L$(libdir)
INCLUDES = -I$(includedir)

EXTRA_DIST = Makefile.win32 README

bin_PROGRAMS = shparticleex
shparticleex_SOURCES = main.cpp ShTrackball.cpp ShTrackball.hpp Camera.cpp Camera.hpp
