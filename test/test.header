// -*- C++ -*-
#include <sh/sh.hpp>
#include <iostream>
#include <fstream>

using namespace SH;

#define NAME(x) {x.name(#x);}

int main(int argc, char** argv)
{
  std::string backendName("sm");
  if (argc > 1) {
    backendName = argv[1];
  }
  
  try {
