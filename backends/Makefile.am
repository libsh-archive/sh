if ENABLE_SM_BACKEND
SM_SUBDIR = sm
else
SM_SUBDIR =
endif

if ENABLE_ARB_BACKEND
ARB_SUBDIR = arb
else
ARB_SUBDIR =
endif

if ENABLE_ATI_BACKEND
ATI_SUBDIR = ati 
else
ATI_SUBDIR =
endif

SUBDIRS = $(SM_SUBDIR) $(ARB_SUBDIR) $(ATI_SUBDIR)

